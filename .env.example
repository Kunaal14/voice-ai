# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Webhook URLs
# Replace with your own webhook endpoints
# These are called when users interact with the landing page

# Form submission webhook (contact form)
FORM_WEBHOOK_URL=https://your-webhook-url.com/webhook/landing-page-form

# Voice agent transcript webhook (saves conversation transcripts)
TRANSCRIPT_WEBHOOK_URL=https://your-webhook-url.com/webhook/landing-page

# Calendar availability webhook (for scheduling appointments)
CALENDAR_AVAILABILITY_URL=https://your-webhook-url.com/webhook/calendar-availability
