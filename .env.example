# Required: Gemini API Key (fallback - prefer using Azure Function)
# For production, use GEMINI_API_KEY_URL instead
GEMINI_API_KEY=your_gemini_api_key_here

# Azure Function URL for secure API key retrieval (recommended for production)
# Format: https://your-function-app.azurewebsites.net/api/gemini-proxy?code=YOUR_KEY
GEMINI_API_KEY_URL=

# Required: Webhook URLs for form submissions
FORM_WEBHOOK_URL=https://your-webhook-url.com/webhook/landing-page-form

# Required: Webhook URL for voice agent transcripts
TRANSCRIPT_WEBHOOK_URL=https://your-webhook-url.com/webhook/landing-page

# Optional: Calendar availability webhook (for scheduling)
CALENDAR_AVAILABILITY_URL=https://your-webhook-url.com/webhook/calendar-availability

# Optional: Cal.com integration
# Format: your-username/your-event-type (e.g., "kunaal/call-30-minute")
# Leave empty if not using Cal.com
CAL_COM_LINK=
